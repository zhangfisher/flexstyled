import { createTheme } from "../../src/theme";

export const theme = createTheme({
    primaryColor   : "#007bff",
    secondaryColor : "blue",
    backgroundColor: "#e8f8ff",
    textColor      : "333",
    fontSize       : "16px",
},{
    prefix:"v"
})

